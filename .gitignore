node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Generated knowledge graph (regenerable via /graphify)
graphify-out/

# BloomCore API's local SQLite store (seeded/regenerable, see server/seed.ts)
server/*.db
server/uploads/

# M5 — dump de rollback runtime (jamais committé)
server/collections.pre-m5.json
